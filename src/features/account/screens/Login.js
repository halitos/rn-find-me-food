import { View, Text } from 'react-native';
import React from 'react';
import { AccountBackground } from '../components/AccountBackground';

const Login = () => <AccountBackground />;

export default Login;
